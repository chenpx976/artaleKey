# ArtaleKey 项目技术总结

## 1. 项目概述

ArtaleKey 是一个专为 MapleStory Worlds 等游戏设计的快捷键管理和游戏辅助工具，主要用于自动化游戏操作和游戏数据识别。

### 1.1 基本信息
- **项目名称**: ArtaleKey
- **版本**: 0.1.2
- **主要技术栈**: Python + PyQt6
- **目标平台**: macOS (主要)、Windows、Linux
- **主要用途**: 游戏快捷键自动化和OCR数据识别

## 2. 核心功能点

### 2.1 快速向上功能 (Quick Up)
- **功能描述**: 通过快捷键触发自动按键序列，模拟连续按键操作
- **触发方式**: 默认组合键 W+↑，可自定义主触发键
- **按键序列**: 按住空格键 → 循环按左键/右键
- **应用场景**: 游戏中的连续移动或技能释放

### 2.2 窗口过滤与监控
- **窗口检测**: 实时监控指定应用窗口的状态
- **窗口激活**: 支持自动激活目标游戏窗口
- **窗口边界获取**: 精确获取窗口坐标和尺寸信息
- **跨平台兼容**: macOS (Quartz/AppKit)、Windows (win32)、Linux (Xlib)

### 2.3 OCR + LLM 智能识别
- **截图功能**: 自动截取游戏窗口内容
- **OCR识别**: 使用 EasyOCR 提取文本信息
- **LLM处理**: 通过大语言模型智能解析游戏数据
- **数据提取**: 自动识别游戏中的经验值、金币、等级等信息

### 2.4 数据存储与管理
- **SQLite数据库**: 存储游戏数据历史记录
- **配置管理**: 统一的配置文件管理系统
- **性能监控**: 内置性能日志和监控系统

### 2.5 用户界面
- **标签页设计**: 分功能模块的界面布局
- **实时状态显示**: 多行状态栏显示实时信息
- **配置界面**: 可视化的参数配置和管理

## 3. 核心技术实现原理

### 3.1 快捷键监听与模拟
```python
# 核心技术: pynput 库
from pynput import keyboard
from pynput.keyboard import Key, Controller

# 全局热键监听
class HotkeyListener(QThread):
    - 监听特定按键组合 (如 W+↑)
    - 长按检测机制 (默认500ms)
    - 事件驱动的按键状态管理
    - 线程安全的按键状态缓存

# 按键模拟
class KeySimulator(QThread):
    - 精确的按键时序控制
    - 可配置的按键间隔 (默认40ms)
    - 安全的按键释放机制
    - 性能优化的循环执行
```

### 3.2 窗口检测技术
```python
# macOS: Quartz 框架
from Quartz import CGWindowListCopyWindowInfo
- 获取所有窗口信息
- 实时监控窗口状态变化
- 精确的窗口边界坐标

# Windows: win32 API
import win32gui, win32process
- 枚举窗口句柄
- 获取窗口进程信息

# Linux: Xlib
from Xlib import display
- X11 窗口系统交互
```

### 3.3 OCR + LLM 识别流程
```python
# 技术栈组合
1. PIL ImageGrab - 窗口截图
2. EasyOCR - 文本识别  
3. LangChain + LLM - 智能数据解析
4. SQLite - 数据持久化

# 处理流程
截图 → OCR文本识别 → LLM结构化解析 → 数据库存储 → UI显示
```

### 3.4 配置管理系统
```python
# 分层配置管理
class ConfigManager:
    - 热键配置 (trigger_key, hold_time, interval)
    - 窗口过滤配置 (target_app, enabled)
    - OCR配置 (api_key, target_window)
    - UI配置 (窗口几何信息)
```

### 3.5 数据库设计
```sql
-- 游戏数据表设计思路
game_data: 
    - timestamp (时间戳)
    - level (等级)
    - experience (经验值) 
    - money (金币)
    - raw_ocr_text (原始OCR文本)
    - processed_data (结构化数据JSON)
```

## 4. 架构设计特点

### 4.1 模块化设计
```
artalekey/
├── core/           # 核心功能模块
│   ├── hotkey_manager.py      # 热键管理
│   ├── window_detector.py     # 窗口检测
│   ├── enhanced_ocr.py        # OCR+LLM处理
│   ├── database.py            # 数据存储
│   └── config.py              # 配置管理
├── ui/             # 用户界面模块
│   ├── tabs/                  # 功能标签页
│   └── components.py          # UI组件库
```

### 4.2 信号槽机制
- PyQt6 信号槽实现模块间通信
- 事件驱动的异步处理
- 松耦合的组件设计

### 4.3 线程管理
- QThread 实现并发处理
- 热键监听独立线程
- 按键模拟独立线程
- OCR处理独立线程

### 4.4 错误处理与日志
- 统一的异常处理机制
- 分级日志系统 (INFO/WARNING/ERROR)
- 性能监控和内存使用跟踪

## 5. Electron 实现方案建议

### 5.1 技术栈选择
```javascript
主要技术:
- React/Vue.js (前端UI框架)
- Node.js (后端逻辑)
- robotjs (按键模拟) 
- node-window-manager (窗口管理)
- tesseract.js (OCR识别)
- OpenAI API (LLM处理)
- sqlite3 (数据存储)
```

### 5.2 核心模块对应关系

#### 5.2.1 热键管理
```javascript
// 替代: artalekey/core/hotkey_manager.py
const globalShortcut = require('electron').globalShortcut;
const robotjs = require('robotjs');

// 全局快捷键注册
globalShortcut.register('CommandOrControl+W+Up', () => {
    // 开始按键模拟
    startKeySimulation();
});

// 按键模拟函数
function startKeySimulation() {
    robotjs.keyDown('space');
    const interval = setInterval(() => {
        robotjs.keyTap('left');
        setTimeout(() => robotjs.keyTap('right'), 40);
    }, 80);
}
```

#### 5.2.2 窗口检测
```javascript
// 替代: artalekey/core/window_detector.py  
const windowManager = require('node-window-manager');

// 获取活动窗口
function getActiveWindow() {
    const activeWindow = windowManager.getActiveWindow();
    return {
        title: activeWindow.getTitle(),
        pid: activeWindow.processId,
        bounds: activeWindow.getBounds()
    };
}

// 监控指定窗口
function monitorTargetWindow(targetTitle) {
    setInterval(() => {
        const windows = windowManager.getWindows();
        const targetWindow = windows.find(w => 
            w.getTitle().includes(targetTitle)
        );
        if (targetWindow) {
            // 窗口存在处理逻辑
        }
    }, 1000);
}
```

#### 5.2.3 OCR + LLM 处理
```javascript
// 替代: artalekey/core/enhanced_ocr.py
const screenshot = require('screenshot-desktop');
const Tesseract = require('tesseract.js');
const OpenAI = require('openai');

// 截图 + OCR + LLM 处理链
async function processGameWindow() {
    // 1. 截图
    const imageBuffer = await screenshot({format: 'png'});
    
    // 2. OCR识别
    const ocrResult = await Tesseract.recognize(imageBuffer, 'eng');
    
    // 3. LLM结构化处理
    const openai = new OpenAI();
    const completion = await openai.chat.completions.create({
        model: "gpt-4",
        messages: [{
            role: "user", 
            content: `解析游戏数据: ${ocrResult.data.text}`
        }]
    });
    
    return JSON.parse(completion.choices[0].message.content);
}
```

#### 5.2.4 数据存储
```javascript
// 替代: artalekey/core/database.py
const sqlite3 = require('sqlite3').verbose();

class GameDatabase {
    constructor() {
        this.db = new sqlite3.Database('gamedata.db');
        this.initTables();
    }
    
    initTables() {
        this.db.run(`
            CREATE TABLE IF NOT EXISTS game_data (
                id INTEGER PRIMARY KEY,
                timestamp TEXT,
                level INTEGER,
                experience TEXT,
                money TEXT,
                raw_data TEXT
            )
        `);
    }
    
    saveGameData(data) {
        this.db.run(`
            INSERT INTO game_data 
            (timestamp, level, experience, money, raw_data) 
            VALUES (?, ?, ?, ?, ?)
        `, [data.timestamp, data.level, data.experience, data.money, JSON.stringify(data)]);
    }
}
```

### 5.3 UI 框架实现
```javascript
// 主窗口结构 (对应 TabbedMainWindow)
const { app, BrowserWindow, ipcMain } = require('electron');

function createMainWindow() {
    const mainWindow = new BrowserWindow({
        width: 800,
        height: 650,
        webPreferences: {
            nodeIntegration: true,
            contextIsolation: false
        }
    });
    
    mainWindow.loadFile('index.html');
    return mainWindow;
}

// React/Vue 组件结构
// - QuickUpTab (快速向上功能)
// - OCRTab (OCR识别功能)  
// - SettingsTab (设置界面)
// - LogsTab (日志查看)
```

### 5.4 配置管理
```javascript
// 替代: artalekey/core/config.py
const Store = require('electron-store');

class ConfigManager {
    constructor() {
        this.store = new Store({
            defaults: {
                hotkey: {
                    trigger_key: 'w',
                    hold_time: 500,
                    interval: 40
                },
                ocr: {
                    api_key: '',
                    target_window: 'MapleStory Worlds'
                }
            }
        });
    }
    
    get(key) {
        return this.store.get(key);
    }
    
    set(key, value) {
        this.store.set(key, value);
    }
}
```

### 5.5 进程通信
```javascript
// 主进程与渲染进程通信 (替代 PyQt6 信号槽)
// 主进程
ipcMain.handle('start-key-simulation', async () => {
    return await startKeySimulation();
});

ipcMain.handle('capture-screenshot', async () => {
    return await captureAndProcessScreenshot();
});

// 渲染进程  
const { ipcRenderer } = require('electron');

async function triggerKeySimulation() {
    const result = await ipcRenderer.invoke('start-key-simulation');
    updateUI(result);
}
```

### 5.6 打包与分发
```json
// package.json
{
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder",
    "build-mac": "electron-builder --mac",
    "build-win": "electron-builder --win"
  },
  "build": {
    "appId": "com.example.artalekey",
    "productName": "ArtaleKey",
    "directories": {
      "output": "dist"
    },
    "mac": {
      "category": "public.app-category.games"
    },
    "win": {
      "target": "nsis"
    }
  }
}
```

## 6. 关键实现注意事项

### 6.1 权限处理
- **macOS**: 需要申请辅助功能权限用于全局快捷键
- **Windows**: 可能需要管理员权限用于某些系统API
- **截图权限**: 需要屏幕录制权限

### 6.2 性能优化
- 使用事件驱动而非轮询机制
- 适当的缓存策略（窗口边界、OCR结果）
- 异步处理避免UI阻塞
- 内存管理和资源释放

### 6.3 错误处理
- 网络请求失败的重试机制
- OCR识别失败的降级处理
- 窗口检测失败的备用方案
- 数据库连接异常的恢复机制

### 6.4 用户体验
- 实时状态反馈
- 配置热更新
- 快捷键冲突检测
- 详细的日志信息

## 7. 总结

ArtaleKey 是一个功能完善的游戏辅助工具，核心技术包括：
1. **全局热键监听与按键模拟**
2. **跨平台窗口管理**  
3. **OCR + LLM 智能数据提取**
4. **SQLite数据持久化**
5. **PyQt6模块化UI设计**

使用 Electron 实现时，主要需要替换的核心依赖：
- `pynput` → `robotjs` + `electron.globalShortcut`
- `PyQt6` → `React/Vue.js` + `Electron`
- `Quartz/win32` → `node-window-manager`
- `EasyOCR` → `tesseract.js`
- `LangChain` → `OpenAI API` 直接调用

整体架构保持不变，通过 IPC 通信替代 PyQt6 的信号槽机制，可以实现完全相同的功能体验。
